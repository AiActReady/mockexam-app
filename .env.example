# Supabase — from your project's Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only — never expose to the client. Used for admin/seed scripts only.
SUPABASE_SERVICE_ROLE_KEY=

# AI tutor
ANTHROPIC_API_KEY=
